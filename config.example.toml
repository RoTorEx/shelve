# Folders scanned by `shelve move` when no input path is provided.
version = 2
inboxes = ["~/Desktop", "~/Downloads"]

# A section has a stable root, opened with `<sector>0`. Every visible folder is
# an explicit relative item below that root, in menu order.
[[sections]]
root = "~"
items = ["Desktop", "Downloads", "Documents"]
